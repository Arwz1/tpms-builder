"""Standalone features.

Every subpackage here is usable on its own with nothing but numpy — no Qt, no viewport,
no application object. ``tpms/cli.py`` and ``tests/test_standalone.py`` exist to keep
that true.
"""
